package tools

// This package will contain tool handler implementations
// For Phase 1, all handlers are implemented directly in the server package
// to keep things simple. In future phases, we can refactor to separate files.
